{{- if and .Values.lessonsapi.enabled .Values.lessonsapi.autoscaling.enabled }}
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: {{ .Release.Name }}-lessonsapi
  namespace: {{ .Release.Namespace }}
  labels:
    app: {{ .Release.Name }}-lessonsapi
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ .Release.Name }}-lessonsapi
  minReplicas: {{ .Values.lessonsapi.autoscaling.minReplicas }}
  maxReplicas: {{ .Values.lessonsapi.autoscaling.maxReplicas }}
  metrics:
    {{- if gt (int .Values.lessonsapi.autoscaling.targetCPUUtilizationPercentage) 0 }}
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: {{ .Values.lessonsapi.autoscaling.targetCPUUtilizationPercentage }}
    {{- end }}
    {{- if gt (int .Values.lessonsapi.autoscaling.targetMemoryUtilizationPercentage) 0 }}
    - type: Resource
      resource:
        name: memory
        target:
          type: Utilization
          averageUtilization: {{ .Values.lessonsapi.autoscaling.targetMemoryUtilizationPercentage }}
    {{- end }}
  {{- if .Values.lessonsapi.autoscaling.behavior }}
  behavior:
    {{- toYaml .Values.lessonsapi.autoscaling.behavior | nindent 4 }}
  {{- end }}
{{- end }}
